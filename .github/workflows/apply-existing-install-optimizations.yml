name: Apply additional-user optimizations

on:
  push:
    branches: [main]
    paths:
      - scripts/apply-existing-install-optimizations.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Apply installer update
        run: python3 scripts/apply-existing-install-optimizations.py
      - name: Validate installer syntax
        run: bash -n install.sh
      - name: Commit installer update
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add install.sh
          git commit -m "Skip repeated setup for additional users" || exit 0
          git push origin main
