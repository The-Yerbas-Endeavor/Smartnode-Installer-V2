name: Apply Smartnode status health check

on:
  push:
    branches: [main]
    paths:
      - scripts/apply-smartnode-status-check.py

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Apply installer update
        run: python3 scripts/apply-smartnode-status-check.py

      - name: Validate shell syntax
        run: bash -n install.sh

      - name: Commit installer update
        run: |
          if git diff --quiet -- install.sh; then
            echo "No installer changes were required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add install.sh
          git commit -m "Check Smartnode status after RPC startup"
          git push origin main
